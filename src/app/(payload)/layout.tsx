// Minimal layout for Payload admin route group.
// Payload's admin panel manages its own HTML structure internally.
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
